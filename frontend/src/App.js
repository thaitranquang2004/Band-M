import logo from "./logo.svg";
import "./App.css";

function App() {
  return (
    <div className="bandm-chat">
      {" "}
      {/* CSS vanilla test */}
      <h1>Band M Chat – No Tailwind, Clean!</h1>
      <p>Build success, ready for Socket.io + Axios.</p>
      <button style={{ background: "blue", color: "white", padding: "10px" }}>
        Inline Style Test
      </button>
    </div>
  );
}

export default App;
